## 20260723 物品页 UI 收口

### 本轮调整

- 物品新增页、编辑页改成更统一的单卡布局，图片区、基础属性、补充属性放到同一视觉容器里，去掉了分区标题。
- 新建/编辑时的初始字段收缩为图片、物品名称、分类、数量，品牌和规格不再作为默认预填充项出现。
- 编辑页顶部撤销提示文案已移除，底部保存修改按钮改为仅在存在未保存修改时显示。
- 顶部区域与屏幕边界间距继续收紧，整体更贴近预期的表单页视觉。

### 备注

- 本轮仍按用户要求不执行编译，由用户本地继续验证。
