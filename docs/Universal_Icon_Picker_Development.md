# 系统级通用图标选择器开发文档 (Universal Icon Picker Development)
# 20260611 13:054第七轮开发
## 实现效果
修改

- 在 MiaoIconPicker.kt 里把弹窗结构改成“标题 / 内容 / 按钮”分别控制间距，避免按钮区继承整段内容间隔。
- 分类管理页的编辑/新建分类弹窗已经走这个紧凑弹窗： CategoryManagementScreen.kt 。
- 新增/编辑页里的分类创建弹窗也已经走同一套结构： ItemFormComponents.kt 。
- CategoryManagementScreen 里仍有一个 AlertDialog ，但那是删除确认弹窗，不是图标选择这一块。

验证

- 我检查了 MiaoIconPicker.kt 、 CategoryManagementScreen.kt 和 ItemFormComponents.kt ，当前无新增诊断错误。
- 这次改动是对弹窗底部动作区本身下手，不是再去碰导航栏，所以会比前一次更有效。

# 20260611 11:07 第六轮开发
## 实现效果
本轮修改

- 修复搜索提示文字被遮挡 ：把内嵌模式下图标筛选输入框高度从 44.dp 调回到 48.dp ，并保持更小字号的占位文字，解决“搜索表情或图标”下半部分被裁掉的问题。
- 缩小导航栏到底部按钮的垂直间距 ：把图标导航栏在内嵌模式下的上下 padding 继续压缩，尤其是底部间距从 8.dp 收到 2.dp ，让它更贴近“取消/确认”按钮，同时把更多垂直空间还给图标列表。
对应代码

- 调整都在 MiaoIconPicker.kt
#### 改进建议
1. 缩小导航栏到底部按钮的垂直间距 还可以再小些，现在还是太多了，可以调整为：
------------------------------
| 新增分类                    |    
| 图标 输入框（请输入分类名称） |    
|----------------------------|    
| 图标选择器                  |    
| 表情符号 | 图标 | 上传    X  |    
| 搜索框                 随机  |   
| （图片显示区域）             |    
|                            |
|                            |
|                            |
|————————————————————————————|
|  图标导航栏                 |     
|----------------------------| ← 图标导航栏和底部两个操作按钮的间距还可以再小些
|                  取消  确认 |
------------------------------


# 20260611 10:45 第五轮开发
## 实现效果
本轮调整

- 分类名称输入框更矮 ：分类管理弹窗和分类选择器新建分类弹窗里的名称输入框都改成了更紧凑的高度 54.dp ，同时占位文字也缩到 bodySmall 。
- 图标筛选输入框更矮 ：内嵌图标选择器里的搜索框继续压缩。
  - 内嵌模式： 44.dp
  - 独立弹层模式： 52.dp
- 长度保持不变 ：两类输入框依然保持原来的横向占位逻辑，没有缩短。
对应代码

- 分类管理弹窗输入框调整： CategoryManagementScreen.kt
- 分类选择器新建分类弹窗输入框调整： ItemFormComponents.kt
- 图标筛选输入框调整： MiaoIconPicker.kt
### 改进建议
1. 图标筛选输入框的提示内容：搜索表情或图标， 现在文字下半部分被遮挡了
2. 图标导航栏可以和取消、确认按钮的垂直间距再小些，扩大图标列表的显示区域

# 20260611 09:15 第四轮开发
## 实现效果
本轮调整

- 弹窗标题缩小 ：新增/编辑分类弹窗标题改成更克制的 titleMedium ，不再显得偏大。
- 图标与输入框同步收紧 ：图标预览从 64.dp 缩到 54.dp ，图标圆角和图标字号也同步缩小；输入框保留原有长度逻辑，但视觉体积与图标更一致。
- 去掉“图标选择器”标题 ：展开后不再额外显示这行标题，直接进入选择器内容区。
- 压缩内嵌控件尺寸 ：在内嵌模式下， 表情符号 | 图标 | 上传 | X 、搜索框、随机按钮、导航图标都缩了一档。
- 图标显示区容纳更多内容 ：内嵌模式下把 Emoji 网格单元从 48.dp 缩到 40.dp ，同时减小网格内外边距和单元间距，因此同样高度下能显示更多图标。
- 强化分区结构 ：在图标显示区和底部导航栏之间加入了更明显的分隔线，并拉开了垂直间距，让结构更像您画的那种“同一面板内分区”而不是两块拼接。
对应代码

- 内嵌图标选择器的紧凑模式调整在 MiaoIconPicker.kt
- 分类管理页弹窗头部与内嵌区调整在 CategoryManagementScreen.kt
- 分类选择器新建分类弹窗同步调整在 ItemFormComponents.kt
### 改进建议
1. 分类名称输入框和图标筛选输入框长度不变，但是高度应该还要更小些


# 20260610 18:44 第三轮开发
## 实现效果
改动结果

- 去掉了“+选择图标” ：现在新增/编辑分类弹窗里只保留图标预览位，用户直接点击图标即可展开选择器。
- 内嵌区域一体化 ：图标选择器不再显示为一张独立的“第二面板”，而是并入弹窗正文结构，变成“分隔线 + 标题 + 选择内容区”的一体化布局，更接近您画的目标效果。
- 统一两处场景 ：分类管理弹窗和分类选择器里的新建分类弹窗都同步做了同样处理。
对应代码

- 图标选择器支持内嵌模式： MiaoIconPicker.kt
- 分类管理页弹窗已改为一体化内嵌： CategoryManagementScreen.kt
- 分类选择器的新建弹窗已改为一体化内嵌： ItemFormComponents.kt
### 改进建议
1.我直接以形象的方式在下面体现，具体的修改建议我在代码右侧标识
不点击图标之前：
------------------------------
| 新增分类                    |    ← 字体缩小些
| 图标 输入框（请输入分类名称） |    ← 图标可以高和宽度等比例缩小，分类名称数据框长度不变，高度得和图标的高度同步缩小些。
|                            |
|                  取消  确认 |
------------------------------
点击图标之后：
------------------------------
| 新增分类                    |    ← 字体和不点击一样，缩小些
| 图标 输入框（请输入分类名称） |    ← 图标也一样，图标可以高和宽度等比例缩小，分类名称数据框长度不变，高度得和图标的高度同步缩小些。
|----------------------------|    ← 缩小其他功能按钮和文字元素等等，让图标选择器可以再整体占位大小不变化的情况下，放入更多的图标
| 图标选择器                  |    ← 这个标题就不要了
| 表情符号 | 图标 | 上传    X  |    ← 这几个按钮的高度稍稍缩小些
| 搜索框                 随机  |   ← 输入框和随机按钮高度稍稍缩小些
| （图片显示区域）             |    ← 图标显示区域现在可以显示更多图标
|                            |
|                            |
|                            |
|————————————————————————————|
|  图标导航栏                 |     ← 图标导航栏和图标显示区域垂直间隔加大点，然后做一个明显一些线隔开
|----------------------------|
|                  取消  确认 |
------------------------------


# 20260610 10:08 第二轮开发
## 实现效果
结论

- 分类策略 ：应该以 Google/Noto/Unicode 的官方分类 作为基线，而不是长期维护一套完全自定义分类。
- 弹窗层级 ：您说得对，图标选择器不应该再以独立 BottomSheet 出现在分类弹窗下层，而应该 内嵌在新增/编辑分类弹窗内部 。
我已修复

- 原来的问题根源是 MiaoIconPicker.kt:L59-L93 采用的是 ModalBottomSheet ，它和 AlertDialog 不是同一层级，所以会出现“图标选择器在分类弹窗下面”的错位。
- 现在我把它拆成了：
  - 独立弹层版 ： MiaoIconPickerSheet
  - 可内嵌面板版 ： MiaoIconPickerPanel
- 分类管理页的新增/编辑分类弹窗已经改成 在弹窗正文里展开图标选择区 ： CategoryManagementScreen.kt:L693-L775
- 分类选择器里的新建分类弹窗也同步改成了 内嵌式 ： ItemFormComponents.kt:L1223-L1305
关于 Google 官方分类

- 当前第一阶段的数据仍是临时整理版，定义在 IconPickerModels.kt:L101-L126 。
- 更合理的正式方案是：
  - 直接采用官方大类 ：Smileys & Emotion、People & Body、Animals & Nature、Food & Drink、Travel & Places、Activities、Objects、Symbols、Flags
  - 系统额外增加一层业务分类 ：最近使用
- 也就是说： 官方分类做底座，最近使用做系统增强 。这是更接近最佳实践的方案。
- 下一步会把 IconPickerModels.kt 的 Emoji 分类正式切换为 Google/Noto 官方分类结构 。
### 改进建议
1. 新增/编辑分类弹窗的“+选择图标”去掉，直接默认就是点击图标，然后弹出图标选择器。
2. 现在这种嵌入的我觉得不太好看，完全就是两个独立的面板。现在是这样：
点击图标后：
点击图标后：
------------------------------
| 新增分类                    |
| 图标 输入框（请输入分类名称） |
| + 选择图标                  |
| -------------------------- | 
| |图标选择器            | |
| |                        | |
| |                        | |
| |                        | |
| |                        | |
| -------------------------- |
|                  取消  确认 |
------------------------------

我希望改造成：
------------------------------
| 新增分类                    |
| 图标 输入框（请输入分类名称） |
|----------------------------| 
| 图标选择器                  |
| （图片显示区域）             |
|                            |
|                            |
|                            |
|----------------------------|
|                  取消  确认 |
------------------------------
这样看起来就和谐多了，统一多了

# 20260609 17:08 第一轮开发
## 实现效果
1. 统一数据模型与渲染器

- IconSource ：在 IconPickerModels.kt 中定义了 IconSource 密封类，支持 Emoji、Vector、Custom 图片的统一解析与存储（通过 EMOJI: 等前缀区分）。
- MiaoIcon ：创建了全局通用的 MiaoIcon 渲染组件，它能自动根据存储的字符串判断图标类型并完成渲染，确保了全系统图标显示的鲁棒性。
2. 标准化 Emoji 资源库

- 分门别类 ：将原本零散的 Emoji 整理为“常用”、“人物与表情”、“自然与动物”、“食物与饮品”等 8 大标准分类。
- 扩展性 ：资源结构完全解耦，未来只需在 CATEGORIZED_EMOJIS 数组中添加项即可完成扩充。
3. “Notion 级”图标选择器组件

- MiaoIconPickerSheet ：在 MiaoIconPicker.kt 中实现了全新的选择器：
  - 分段切换 ：顶部支持“表情符号”、“图标”、“上传”三段式 Tab 切换。
  - 锚点导航 ：底部集成分类锚点栏（含 🕒 最近使用、😀 表情等），点击可实现列表平滑滚动。
  - 实时搜索 ：支持对全量 Emoji 进行关键词过滤。
  - 随机功能 ：点击“随机”按钮可快速生成一个有趣的图标。
4. 全系统深度集成

- 分类管理页 ： CategoryManagementScreen.kt 已全面接入。现在点击分类图标会唤起全新的 MiaoIconPickerSheet ，且弹窗内的图标预览位支持点击触发。
- 分类选择器 ： ItemFormComponents.kt 及其新建分类弹窗也已同步升级，保持了跨页面的交互一致性。

## 问题
1. 谷歌对于emoji和图标是有分类的，直接使用这个分类呢
2. 图标选择器和新增分类/新增子分类/编辑分类的嵌入有问题：现在点击新增分类/新增子分类/编辑分类,然后再点击图标进行图标选择，实际上应该就在新增分类/新增子分类/编辑分类的弹窗的上一层进行图标选择。
现在实现点击图标后，图标选择器在新增分类/新增子分类/编辑分类的弹窗的下一层，这是不正确的。
实际上我们要实现的应该是：
初始时，新增分类/新增子分类/编辑分类的弹窗结构是（以新增分类为例），
------------------------------
| 新增分类                    |
| 图标 输入框（请输入分类名称） |
|                            |
|                  取消  确认 |
------------------------------
然后点击图标，图标选择器界面会嵌入在此弹窗，用户可以在图标选择器中选择图标。
点击图标后：
------------------------------
| 新增分类                    |
| 图标 输入框（请输入分类名称） |
| -------------------------- | 
| |图标选择器            | |
| |                        | |
| |                        | |
| |                        | |
| |                        | |
| -------------------------- |
|                  取消  确认 |
------------------------------
