# 规则绑定页设计文档 v1

## 0. 2026-07-22 设计重构说明

以下正文保留的是上一阶段“从属性出发绑定规则”的设计。
从 2026-07-22 起，规则绑定的主入口正式调整为“物品规则页”，而不再是“属性详情页 -> 添加规则”。

新的产品语义如下：

1. 规则定义只定义：
   - 槽位
   - 公式
   - 哪些槽位是输出
2. 槽位本身不带具体值，它只是占位。
3. 具体值来自物品上的属性。
4. 因此真正的绑定动作发生在：
   - `物品新增/编辑页 -> 规则 Tab -> 某条规则实例`

新的规则绑定页/区域应该回答的是：

- 这个物品启用了哪些规则
- 每条规则的哪些槽位已经绑定到物品属性
- 哪些必填槽位还未绑定
- 哪些槽位是输出，并写回到哪个属性

页面结构同步调整为：

- 物品页拆为两个 Tab：
  - `属性`
  - `规则`
- 规则 Tab 中展示该物品的全部规则实例。
- 每条规则实例卡片展示：
  - 规则名
  - 槽位绑定摘要
  - 未完成状态
  - 编辑入口
- 若某条规则实例仍有必填槽位未绑定，则卡片需高亮提示，例如浅红边框。

因此，旧版“从属性进入规则绑定向导”的设计不再作为后续主路径，只保留为历史参考。

## 1. 文档定位

本文档用于定义 V1 阶段的“规则绑定页”。

这里的规则绑定页，不是规则定义编辑页，而是：

- 用户从某个属性出发
- 选择一条规则
- 让当前属性占据一个兼容槽位
- 再补齐这条规则的其他槽位
- 最终形成规则绑定实例

本文档重点回答：

- 为什么绑定页必须独立于规则编辑页
- 从属性出发时，系统应该如何限制可选规则和可选槽位
- 其他槽位如何按类型分别补齐
- 快捷创建依赖属性如何进入正式流程

---

## 2. 页面定位

规则绑定页负责“组装一条规则绑定实例”。

它负责：

- 选择规则
- 过滤当前属性可进入的槽位
- 把当前属性绑定到入口槽位
- 配置剩余的属性输入槽位
- 配置配置输入槽位
- 配置系统输入槽位
- 配置输出槽位
- 做完整性校验
- 保存绑定实例

它不负责：

- 定义规则母版
- 修改规则模板结构
- 查看某个物品上的运行结果
- 直接编辑规则表达式

固定原则：

- `编辑页定义规则`
- `绑定页使用规则`

---

## 3. 为什么绑定页必须单独存在

如果把规则定义和规则绑定混在一起，会产生三类问题：

1. 用户看不清自己是在“定义规则”还是“使用规则”。
2. 规则定义会被具体属性名污染，失去复用性。
3. 绑定实例中的配置输入、系统输入、输出落点无法清晰表达。

因此 V1 必须把两件事拆开：

- 规则定义页只定义规则母版
- 规则绑定页只完成具体实例组装

---

## 4. 入口场景

第一阶段最主要的入口场景是：

- 用户在属性管理页，从某个属性出发点击 `添加规则`

例如：

- 当前属性：`QQ会员价格`
- 用户点击：`添加规则`

此时系统进入规则绑定流程，而不是直接打开规则定义编辑页。

---

## 5. 页面流程

规则绑定页建议采用向导式流程，固定为以下步骤：

1. 选择规则
2. 选择入口槽位
3. 补齐剩余槽位
4. 预览绑定结果
5. 保存绑定实例

推荐结构：

```text
规则绑定
Step 1: 选择规则
Step 2: 入口槽位
Step 3: 补齐剩余槽位
Step 4: 预览与保存
```

### 5.1 页面信息架构建议

规则绑定页建议采用“顶部步骤条 + 主内容区 + 底部操作栏”的向导结构：

```text
顶部
- 返回
- 页面标题
- Stepper

主内容区
- 当前属性摘要
- 当前步骤主内容
- 右侧或底部提示区

底部操作栏
- 上一步
- 下一步 / 保存
- 取消
```

当前属性摘要建议常驻展示：

- 当前属性名称
- 当前属性值类型
- 当前属性所在页面上下文
- 当前已选择的规则与入口槽位

这样可以避免用户在多步向导中丢失“我是从哪个属性出发配置规则”的上下文。

### 5.2 步骤推进规则

向导每一步都应有明确的推进条件，而不是允许用户无约束跳步：

- 未选规则时不能进入 `入口槽位`
- 未选入口槽位时不能进入 `补齐剩余槽位`
- 存在阻止保存错误时不能进入最终保存

同时建议允许：

- 返回上一步修改
- 在 `补齐剩余槽位` 阶段实时查看当前完成度
- 编辑已有实例时直接进入 `补齐剩余槽位` 或 `预览与保存`

### 5.3 草稿与中断恢复

绑定页属于“选择较多、上下文较强”的流程页，建议从第一版就明确草稿策略：

- 未保存离开时提示用户放弃或继续编辑
- 若当前属性草稿页已经承接绑定结果，则允许将绑定向导结果先回写到属性草稿
- 绑定实例在未完整前可作为向导草稿存在，但不进入正式运行时

这与当前“保存后先返回属性草稿页，再随属性一起保存”的流程保持一致。

---

## 6. 第一步：选择规则

## 6.1 规则筛选逻辑

并不是所有规则都能绑定到当前属性。

系统应先过滤出：

- 至少存在一个输入槽位
- 且该输入槽位和当前属性类型兼容
- 且来源约束允许当前属性进入

例如：

- 当前属性是数值属性 `QQ会员价格`
- 可命中规则中的槽位：
  - `price`
  - `subscriptionPrice`
  - `amount`
- 不应命中：
  - `chargeDay`
  - `currentDate`
  - `totalPaid`

## 6.2 规则列表展示

在选择规则步骤中，每个规则候选项建议展示：

- 规则名称
- 规则类型
- 可兼容的入口槽位数量
- 输入槽位摘要
- 输出槽位摘要

### 6.3 规则卡片的补充信息

为了帮助用户做选择，规则卡片建议再补以下信息：

- 规则说明
- 该规则是否包含必填依赖属性
- 该规则是否包含输出写回
- 当前属性可进入的槽位名列表

如果某条规则被过滤掉，也建议在需要时支持“查看不可选原因”，例如：

- 当前属性类型不兼容
- 该规则没有允许属性输入的槽位
- 当前规则仅适用于系统输入或配置输入场景

### 6.4 搜索与排序建议

规则候选多起来后，仅靠列表很难选，建议固定支持：

- 按名称搜索
- 按规则类型筛选
- 按“与当前属性兼容度”排序
- 按最近使用排序

其中“与当前属性兼容度”可优先考虑：

- 可兼容入口槽位数量
- 是否存在主语义更贴近的槽位名
- 是否需要额外补齐大量必填槽位

---

## 7. 第二步：选择入口槽位

## 7.1 槽位候选过滤

当用户选中某条规则后，系统不应展示所有槽位，而应只展示：

- 方向为输入槽位
- 值类型与当前属性兼容
- 来源类型允许属性输入
- 不与现有默认占位冲突

## 7.2 展示内容

每个槽位候选建议展示：

- 槽位名称
- 槽位 key
- 槽位值类型
- 是否必填
- 简短说明

### 7.2.1 入口槽位卡片建议

入口槽位卡片除了基础字段外，建议增加以下说明：

- 该槽位在规则中的语义角色
- 绑定当前属性后还需要补齐多少个必填槽位
- 该选择会产生哪些输出类型

例如：

- `price`：主金额输入，还需补齐 `chargeDay`、`startDate`、`cycleStep`
- `amount`：通用数值输入，语义较弱，不推荐优先选择

### 7.2.2 推荐默认项

当一个规则存在多个兼容入口槽位时，系统可以给出推荐项，但不应强行替用户决定。

推荐顺序可参考：

1. 槽位名称与当前属性名称或类型语义更贴近
2. 主业务输入槽位优先于泛化槽位
3. 补齐成本更低的槽位优先

## 7.3 用户操作结果

一旦用户把当前属性绑定到某个槽位，系统就进入：

- `补齐剩余槽位`

这一步完成后，当前属性会成为：

- 入口属性

---

## 8. 第三步：补齐剩余槽位

这是绑定页的核心步骤。

系统应按槽位来源类型分别给出操作方式。

## 8.1 属性输入槽位

对于 `属性输入` 槽位，应支持：

- 选择已有兼容属性
- 直接搜索已有属性
- 快捷创建兼容属性

每个属性输入槽位建议显示：

- 槽位名称
- 需要的值类型
- 是否必填
- 当前绑定结果

### 8.1.1 属性输入槽位交互建议

属性输入槽位建议固定为“槽位卡片 + 选择器”的交互形式，每张卡片包含：

- 槽位标题与说明
- 当前绑定属性
- 搜索已有属性入口
- 快捷创建属性入口
- 兼容性提示

如果某个槽位已经自动推断出较优候选，也应仍然要求用户显式确认或明确看到当前默认绑定结果。

## 8.2 配置输入槽位

对于 `配置输入` 槽位，应支持：

- 直接填写配置值
- 使用默认值

例如：

- `startDate`
- `cycleStep`
- `cycleUnit`

这类输入不会产生新的属性依赖节点。

### 8.2.1 配置输入控件策略

配置输入不应统一用纯文本框处理，而应按值类型给出适配控件：

- 数值 -> 数字输入
- 日期 -> 日期选择
- 布尔 -> 开关或单选
- 枚举 -> 下拉选择
- 文本 -> 文本输入

如果规则定义中已经存在默认值或推荐值，建议直接展示并允许用户一键采用。

## 8.3 系统输入槽位

对于 `系统输入` 槽位，应支持：

- 选择系统变量
- 使用规则默认系统变量

例如：

- `currentDate -> CURRENT_DATE`

### 8.3.1 系统输入展示策略

系统输入槽位建议优先展示“推荐默认值”，例如：

- `currentDate -> CURRENT_DATE`
- `currentUserId -> CURRENT_USER_ID`

用户通常不需要频繁修改这类值，因此：

- 有稳定默认值时直接预填
- 无可用默认值时才要求用户显式选择
- 当前运行时不可用的系统变量应显示原因，而不是静默消失

## 8.4 输出槽位

对于输出槽位，应根据来源类型分别处理：

- `属性输出`
  - 选择已有兼容属性
  - 快捷创建兼容输出属性
- `只读输出`
  - 无需配置落点
- `系统输出`
  - 选择系统变量目标

### 8.4.1 输出槽位的展示重点

输出槽位是用户理解“这条规则最终影响什么”的关键，因此卡片建议重点展示：

- 输出名称
- 输出类型
- 写回方式或输出目标
- 若不配置会导致什么结果

其中：

- `属性输出` 重点是“写回哪个属性”
- `只读输出` 重点是“结果会在什么页面展示”
- `系统输出` 重点是“结果会写到哪个系统变量”

### 8.5 补齐页的整体布局建议

补齐剩余槽位时，建议按来源类型分组，而不是把所有槽位混成一个长列表：

1. 属性输入
2. 配置输入
3. 系统输入
4. 输出槽位

每组都建议展示：

- 已完成数量 / 总数量
- 必填未完成数量
- 当前错误数

这样用户能更快定位“为什么我还不能保存”。

---

## 9. 快捷创建属性的正式流程

快捷创建属性是 V1 的核心便捷能力，不应只做成一个小技巧。

推荐流程如下：

1. 用户在某个属性槽位没有找到合适属性。
2. 用户直接输入名称，例如 `订阅日`。
3. 系统校验名称是否可用。
4. 系统按照槽位值类型和默认预设创建属性。
5. 系统自动将新属性绑定到当前槽位。
6. 规则绑定流程不中断，继续停留在当前向导中。

固定规则：

- 仅 `属性输入` 和 `属性输出` 槽位允许快捷创建属性
- 创建的属性必须继承槽位类型约束
- 创建成功后自动与当前规则绑定实例关联

### 9.1 快捷创建后的回写行为

快捷创建属性成功后，绑定页需要同步更新三部分状态：

- 当前向导的槽位绑定结果
- 属性草稿中的新增属性定义
- 依赖补齐与后续保存所需的上下文缓存

这样可以避免出现“看起来创建成功了，但保存绑定时找不到新属性”的断链问题。

### 9.2 创建失败时的处理

快捷创建失败时，不应直接退出绑定流程。

建议处理方式：

- 保留用户当前已填写的名称
- 明确提示失败原因，例如名称重复、类型不兼容、草稿状态异常
- 允许用户直接改名重试或改为选择已有属性

---

## 10. 第四步：预览与保存

保存前应给用户一个“绑定结果预览”。

建议展示：

- 规则名称
- 入口属性
- 当前属性占据的槽位
- 其他属性输入槽位绑定结果
- 配置输入摘要
- 系统输入摘要
- 输出落点摘要
- 完整性检查结果

## 10.1 完整性判定

一条绑定实例只有在以下条件全部满足时，才允许保存：

1. 所有必填输入槽位都已绑定
2. 所有必填输出槽位都已绑定
3. 全部绑定都通过类型检查
4. 表达式变量都能在当前绑定上下文中解析

未通过校验时，应明确提示缺失槽位，而不是只提示“保存失败”。

### 10.2 校验反馈分层

绑定页的校验建议也分层处理：

1. 阻止保存错误
2. 允许保存警告
3. 提示信息

阻止保存错误例如：

- 必填槽位未绑定
- 绑定目标类型不兼容
- 输出槽位缺失
- 必需系统变量当前不可用

允许保存警告例如：

- 某些可选槽位未配置
- 当前选择的入口槽位语义较弱
- 存在多个输出但用户尚未充分理解写回影响

### 10.3 保存成功后的返回语义

绑定页保存成功后，建议固定返回到属性草稿或属性详情上下文，并同步更新：

- 当前属性的规则绑定摘要
- 依赖补齐说明
- 输出影响摘要

如果是编辑已有绑定实例，则保存后应替换原实例，而不是额外新增一条重复绑定。

---

## 11. 删除和编辑已有绑定实例

绑定页除了新建，也需要承接后续编辑。

编辑已有绑定实例时，应支持：

- 修改入口槽位之外的剩余槽位绑定
- 替换依赖属性
- 替换输出属性
- 修改配置输入
- 修改系统输入

删除绑定实例时，应明确说明：

- 会让当前属性失去哪条规则效果
- 会影响哪些派生属性或只读输出

### 11.1 编辑已有绑定实例时的限制

编辑已有绑定实例时，建议补充以下限制：

- 若修改入口槽位会导致整条绑定语义重置，应要求用户二次确认
- 若替换输出属性会影响既有派生结果展示，应明确提示
- 若某个旧系统变量已不可用，允许用户先修复后再保存

### 11.2 删除确认文案建议

删除绑定实例时的确认信息建议至少包含：

- 规则名称
- 当前入口属性
- 受影响的输出属性或只读结果
- 删除后的直接后果

例如：

- 删除后，`QQ会员价格` 将不再通过规则 `周期扣费` 自动推导 `累计总价`

---

## 12. 与其他页面的关系

推荐导航关系如下：

```text
属性详情页
-> 添加规则
   -> 规则绑定页
      -> 保存后返回属性详情页

规则管理页
-> 规则详情页
   -> 查看规则被哪些属性绑定
```

说明：

- 规则管理页主要看规则定义
- 规则绑定页主要看具体使用

---

## 13. UI 状态建议

规则绑定页建议使用独立向导状态，而不是直接沿用旧的 `RuleBindingCandidateUiModel`。

推荐方向：

```kotlin
data class RuleBindingWizardState(
    val entryAttributeId: String,
    val entryAttributeName: String,
    val selectedRuleId: String? = null,
    val compatibleRules: List<RuleBindingRuleCandidate> = emptyList(),
    val selectedEntrySlotKey: String? = null,
    val slotBindings: List<RuleSlotBindingDraft> = emptyList(),
    val completionState: RuleBindingCompletionState = RuleBindingCompletionState.Incomplete,
    val message: String? = null,
)
```

其中 `slotBindings` 建议覆盖：

- 属性输入绑定
- 配置输入绑定
- 系统输入绑定
- 输出绑定

### 13.1 建议补充的派生状态

除了基础草稿字段外，建议在 UI 状态中再明确维护：

- 当前步骤
- 是否存在未保存修改
- 当前未完成必填槽位列表
- 当前错误摘要
- 可保存状态

这样可以让：

- Stepper 状态清晰
- 底部按钮逻辑更简单
- 预览页无需重复临时计算所有提示

---

## 14. 第一阶段固定边界

第一阶段建议完成：

1. 从属性出发的规则绑定入口
2. 规则候选过滤
3. 入口槽位过滤
4. 剩余槽位补齐
5. 快捷创建依赖属性
6. 保存绑定实例
7. 基于绑定实例的完整性校验

第一阶段暂不做：

- 从物品编辑页直接修改复杂绑定实例
- 批量重绑
- 绑定实例版本历史
- 绑定实例调试时间线

---

## 15. 最终结论

当前可固定的规则绑定页方案如下：

1. 规则绑定页是 V1 的关键页面，负责组装规则绑定实例。
2. 流程必须从“当前属性”出发，而不是从抽象规则空白开始。
3. 系统必须先过滤可用规则，再过滤可占据槽位，然后引导补齐剩余槽位。
4. 快捷创建属性必须成为正式绑定流程的一部分，而不是临时补丁能力。
5. 绑定页保存的对象是规则绑定实例，这也是依赖推导和删除影响分析的统一来源。
