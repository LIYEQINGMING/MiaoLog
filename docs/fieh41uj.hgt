# 属性模型与规则模型草案

## 0. 2026-07-22 最新收敛说明

本页以下历史内容仍然保留，用于记录前序设计演进。
但从 2026-07-22 起，顶层关系正式收敛为“物品持有属性，物品持有规则实例，属性再绑定到规则槽位”。

当前最新模型如下：

1. `物品 - 属性`
   - 物品是属性的集合。
   - 属性仍然负责定义字段、值类型、来源、交互方式。
2. `物品 - 规则`
   - 规则定义仍然是可复用母版。
   - 真正被物品持有的是“规则实例”，也就是某个物品上配置过的规则绑定结果。
3. `属性 - 规则`
   - 属性本身不再“拥有规则”。
   - 属性只是被用户放入规则的某个槽位，作为输入值来源，或者作为输出回写目标。

由此带来的边界调整：

- `规则定义页`
  - 只负责定义槽位、公式、输出槽位，不绑定具体属性。
- `物品新增/编辑页`
  - 负责配置这个物品有哪些属性。
  - 也负责配置这个物品启用了哪些规则实例。
  - 规则实例里再去完成“槽位 -> 物品属性”的绑定。
- `属性管理页`
  - 继续管理属性定义、规则定义、模板。
  - 不再承担“某个属性直接挂着哪些规则实例”的主持久化语义。

当前阶段正式推荐的持久化方向：

- 属性定义上的 `ruleBindingsJson` 仅作为旧链路兼容层保留，不再继续扩张语义。
- 新链路以 `item_rule_bindings` 这类“物品级规则实例表/模型”为主。
- 规则是否完整，以“必填槽位是否已完成绑定”来判定，而不是以“属性是否带着某条规则”来判定。

面向页面的信息架构同步为：

- 物品新增/编辑页拆成两个 Tab：
  - `属性`
  - `规则`
- 属性页只配置物品属性集合。
- 规则页只配置物品规则实例集合。
- 当某条规则实例还有必填槽位未绑定时，规则卡片需要以浅红边框或等价方式提示“此规则仍需补充属性”。

## 1. 文档目的

本文档用于基于当前最新讨论，形成一份新的顶层草案，用来统一以下问题：

- 什么是属性模型
- 什么是规则模型
- 规则为什么不能再继续停留在“规则名 + 输入角色 + 依赖属性”层面
- 为什么当前阶段不单独建立第三套“关系模型”
- 属性、规则、模板、物品编辑页之间应该如何协作

本文档是顶层草案，不直接替代所有正式规范。

它的目标是先固定主模型，再据此回写：

- `Attribute_Modeling_Spec.md`
- `Attribute_Template_Spec.md`
- `Foundation_Attribute_Template_Catalog.md`

规则模型的专项细化，以 `Rule_Definition_Binding_Design_v1.md` 为准。

---

## 2. 顶层认知

当前系统服务于“物品”。

可以先把整个系统理解成：

- 物品是最终承载数据的实体
- 属性是附着在物品上的字段体系
- 规则是建立在属性之上的跨字段计算机制
- 模板只是帮助用户创建属性或规则的母板

也就是说：

1. 物品是最终承载数据的对象。
2. 属性负责描述物品有哪些字段，以及这些字段怎么填。
3. 规则负责描述多个字段之间如何计算、推导和联动。
4. 模板负责降低创建成本，不是主模型本体。

---

## 3. 当前正式收敛的主模型

当前阶段只正式建立两套主模型：

1. 属性模型
2. 规则模型

当前不单独建立第三套“关系模型”。

原因是：

- 属性之间的关系，本质上可以从规则绑定实例中推导
- 如果单独再抽一套关系模型，系统会迅速变重
- 当前更重要的是先让“简单属性可用、复杂规则可配、依赖可分析”

固定结论：

- `属性负责字段定义`
- `规则负责跨字段计算`
- `关系先隐含在规则绑定中`

---

## 4. 属性模型

## 4.1 属性模型的职责

属性模型只负责一件事：

- 定义物品上可以填写什么字段，以及这个字段怎么填

属性模型不直接负责：

- 跨属性计算
- 周期规则执行
- 多属性之间的依赖推导
- 派生结果的求值

这些统一由规则模型承担。

## 4.2 属性模型包含的对象

属性模型包含两个核心对象：

1. 属性定义
2. 属性值

## 4.3 属性定义是什么

属性定义回答的是：

- 这个属性叫什么
- 这个属性的值本质上是什么类型
- 这个属性有哪些类型专属配置
- 这个属性的值从哪里来
- 这个属性在 UI 上如何交互
- 这个属性关联了哪些规则绑定实例

属性定义是用户在“属性管理”中真正创建和维护的对象。

## 4.4 属性定义的核心字段

当前建议属性定义先严格收敛为以下字段：

1. 属性名
2. 值类型
3. 值属性
4. 值来源
5. 交互方式
6. 规则绑定实例引用

其中：

- `规则绑定实例引用` 不等于把规则逻辑塞进属性
- 属性定义只声明“我参与了哪些规则实例”
- 规则真正的计算逻辑仍然属于规则模型

## 4.5 值类型

值类型回答的是：

- 这个属性值本质上是什么类型

当前建议第一阶段稳定支持：

- 文本
- 数值
- 日期
- 布尔
- 选择

说明：

- `价格` 不是新的值类型
- `价格` 是 `数值` 类型下的一种数值格式

## 4.6 值来源

值来源回答的是：

- 这个属性值最终来自哪里

当前建议固定为三类：

1. `输入`
2. `固定值`
3. `来自系统`

其中：

- `输入`
  - 用户自己录入或选择
- `固定值`
  - 值来自默认配置或固定值池
- `来自系统`
  - 值来自系统变量、规则输出或系统映射结果

## 4.7 交互方式

交互方式回答的是：

- 当前属性在 UI 上通过什么控件和什么方式与用户交互

固定规则如下：

- 交互方式可配置
- 但不能完全自由
- 用户只能在“当前值类型 + 当前值来源 + 当前值属性”支持的交互方式集合中选择

## 4.8 属性值是什么

属性值回答的是：

- 某个物品在这个属性上的实际填写结果

属性值只承载结果，不承载规则。

也就是说：

- 属性值不负责计算平均值
- 属性值不负责计算待付尾款
- 属性值不负责计算周期扣费

这些都属于规则层。

---

## 5. 规则模型

## 5.1 规则模型的职责

规则模型负责：

- 多个属性之间的计算
- 属性与时间之间的计算
- 周期性逻辑
- 衍生结果的推导
- 属性依赖关系的形成
- 删除属性时的影响分析

规则模型不负责：

- 替代属性定义
- 替代属性值
- 替代属性的值类型与交互方式定义
- 承担任意脚本平台职责

## 5.2 规则模型的核心结论

当前阶段正式固定以下结论：

1. 规则最本质上是一个可执行表达式。
2. 用户定义规则时，真正定义的是：
   - 变量槽位
   - 来源类型
   - 绑定方式
   - 表达式
3. 用户使用规则时，真正形成的是“规则绑定实例”，而不是一条模糊的规则名引用。
4. 属性之间的依赖关系，不单独维护一套关系模型，而是从规则绑定实例中推导。

更抽象地说：

`规则 = 槽位化变量定义 + 槽位来源约束 + 绑定实例 + 表达式执行`

## 5.3 规则模型包含的对象

当前建议规则模型正式收敛为四个对象：

1. 规则定义
2. 槽位定义
3. 规则绑定实例
4. 规则输出

其中：

- 规则定义负责描述“这条规则长什么样”
- 槽位定义负责描述“这条规则有哪些输入输出变量”
- 规则绑定实例负责描述“某次使用这条规则时，每个槽位绑定到了什么”
- 规则输出负责描述“规则最终产生了什么，以及结果落到哪里”

---

## 6. 规则定义

## 6.1 规则定义是什么

规则定义回答的是：

- 这条规则叫什么
- 这条规则属于什么类型
- 这条规则有哪些输入槽位和输出槽位
- 这些槽位各自允许什么来源
- 这条规则的表达式是什么
- 这条规则在什么时机触发

规则定义本身不直接绑定某个具体属性。

也就是说：

- 规则定义是可复用的
- 规则定义不和某个具体业务字段写死
- 规则定义只是声明“我需要什么变量”

## 6.2 规则定义的核心字段

第一阶段建议规则定义严格收敛为以下字段：

1. 规则名
2. 规则类型
3. 槽位定义列表
4. 表达式定义
5. 输出策略
6. 触发方式
7. 描述

## 6.3 规则类型

规则类型用于表达规则的大类语义，不直接替代表达式本身。

第一阶段建议支持：

- 差值规则
- 平均值规则
- 周期规则
- 累计规则
- 条件规则
- 自定义表达式规则

说明：

- 规则类型主要用于管理页分类、模板归类和编辑页引导
- 真正的计算逻辑仍以表达式为准

## 6.4 表达式定义

第一阶段规则表达式正式采用“受限表达式语言”思路，不支持任意脚本执行。

表达式要满足以下边界：

- 可读
- 可校验
- 可序列化
- 可安全执行
- 可根据上下文重算

表达式第一阶段建议支持：

- 字面量
  - `1`
  - `"MONTH"`
  - `true`
  - `null`
- 变量引用
  - `price`
  - `startDate`
  - `currentDate`
- 算术运算
  - `+`
  - `-`
  - `*`
  - `/`
  - `%`
- 比较运算
  - `>`
  - `<`
  - `>=`
  - `<=`
  - `==`
  - `!=`
- 逻辑运算
  - `&&`
  - `||`
  - `!`
- 条件函数
  - `if(cond, a, b)`
- 空值处理
  - `coalesce(a, b, c)`
- 日期函数
  - `dateDiff(start, end, "DAY")`
  - `addPeriod(date, 1, "MONTH")`
- 周期函数
  - `cycleCount(start, end, chargeDay, cycleStep, cycleUnit)`
  - `cycleElapsed(start, current, cycleUnit)`
- 数值函数
  - `min`
  - `max`
  - `abs`
  - `round`

固定规则：

- 第一阶段不支持循环
- 第一阶段不支持任意副作用脚本
- 第一阶段不支持在表达式中直接写入属性
- 第一阶段不支持 `+=` 这类命令式写法

规则表达式必须是声明式的。

---

## 7. 槽位定义

## 7.1 槽位定义是什么

槽位定义是规则模型的核心。

槽位定义回答的是：

- 这条规则里有哪些变量
- 每个变量是输入还是输出
- 每个变量是什么类型
- 每个变量允许从哪里取值
- 当前变量是否必须完成绑定

也就是说：

- 用户不是直接给属性随便绑规则
- 而是先让属性进入某条规则允许的槽位

## 7.2 槽位定义的核心字段

第一阶段建议槽位定义包含以下字段：

1. 槽位 key
2. 槽位名称
3. 槽位方向
4. 槽位值类型
5. 槽位来源类型
6. 是否必填
7. 是否允许快捷创建属性
8. 快捷创建属性预设
9. 描述

## 7.3 槽位方向

第一阶段固定为：

1. 输入槽位
2. 输出槽位

其中：

- 输入槽位用于表达式读取
- 输出槽位用于表达式产出

## 7.4 槽位值类型

第一阶段建议规则槽位值类型先与属性值类型体系尽量对齐，并在规则层补充少量系统类型。

建议支持：

- 文本
- 数值
- 日期
- 布尔
- 选择
- 周期单位
- 系统日期时间

## 7.5 槽位来源类型

当前阶段建议固定为以下几类：

1. 属性输入
2. 配置输入
3. 系统输入
4. 属性输出
5. 只读输出
6. 系统输出

统一说明如下：

- `属性输入`
  - 需要绑定到某个具体属性
- `配置输入`
  - 不来自属性，由用户在绑定规则时直接填写
- `系统输入`
  - 来自系统级变量
- `属性输出`
  - 输出结果写回某个属性
- `只读输出`
  - 仅作为运行结果显示
- `系统输出`
  - 输出结果写回系统级变量

## 7.6 快捷创建属性能力

当某个槽位允许快捷创建属性时，用户在补齐规则绑定时，可以：

- 直接输入一个名称
- 系统按照该槽位的值类型与默认预设快速创建属性
- 创建成功后自动把该属性绑定到当前槽位
- 同时把该属性与当前规则实例关联起来

固定规则：

- 仅 `属性输入` 与 `属性输出` 槽位允许开启快捷创建属性
- `配置输入` 与 `系统输入` 不允许快捷创建属性
- 快捷创建属性时必须继承槽位类型约束，不允许创建不兼容类型

---

## 8. 规则绑定实例

## 8.1 规则绑定实例是什么

规则绑定实例回答的是：

- 某次实际使用这条规则时，每个槽位绑定到了什么

这里需要特别强调：

- 绑定实例不等于规则定义
- 规则定义是母版
- 绑定实例是“这条规则在某个属性配置中的一次具体落地”

## 8.2 为什么绑定实例是核心对象

因为用户真正使用规则时，发生的是：

1. 从当前属性出发，选择一条规则
2. 把当前属性绑定到该规则允许的某个输入槽位
3. 再补齐该规则剩余槽位
4. 最终形成一条完整的可执行规则实例

所以系统真正要持久化和分析的，是“规则实例绑定图”，而不是单纯“属性拥有规则名”。

## 8.3 绑定实例的核心字段

第一阶段建议规则绑定实例至少包含：

1. 绑定实例 id
2. 规则定义 id
3. 当前入口属性 id
4. 属性槽位绑定表
5. 配置槽位绑定表
6. 系统槽位绑定表
7. 输出槽位绑定表
8. 创建方式
9. 是否完整
10. 描述

## 8.4 绑定实例中的几类绑定

### 8.4.1 属性槽位绑定

表示：

- 某个输入槽位由哪个属性提供值

### 8.4.2 配置槽位绑定

表示：

- 某个输入槽位不是来自属性，而是直接保存配置值

### 8.4.3 系统槽位绑定

表示：

- 某个输入槽位来自系统级变量

### 8.4.4 输出槽位绑定

表示：

- 某个输出槽位最后写到哪里

## 8.5 绑定完整性的判定

一条绑定实例只有在以下条件全部满足后，才算完整：

1. 所有必填输入槽位都已绑定
2. 所有必填输出槽位都已绑定
3. 所有绑定都通过类型检查
4. 表达式中的变量引用都能在绑定上下文中找到

未完成绑定的规则实例：

- 允许以草稿状态存在
- 不进入运行时执行
- 不计入物品编辑页自动补齐逻辑

---

## 9. 规则输出

## 9.1 规则输出是什么

规则输出回答的是：

- 这条规则最终算出了什么
- 这个结果要显示在哪里或写到哪里

## 9.2 当前建议支持的输出形态

第一阶段建议支持：

1. 只读输出
2. 属性写回输出
3. 系统变量写回输出

同时建议显式声明输出更新方式：

1. 覆盖型
2. 累计型

但第一阶段实际优先落地：

- 只读输出
- 覆盖型属性写回
- 系统变量写回

累计型先在模型中预留，不在第一阶段全面开放。

---

## 10. 依赖关系与页面联动

## 10.1 为什么当前不单独建立关系模型

因为：

- 规则绑定实例本身已经提供了稳定的依赖语义
- 再额外维护一套关系模型会增加复杂度与用户心智负担

所以当前阶段固定为：

- `关系先隐含在规则绑定实例中`

## 10.2 依赖推导规则

从规则绑定实例推导依赖关系时，建议固定以下规则：

1. 当前规则实例中所有 `属性输入` 槽位绑定的属性，互为输入依赖集合。
2. `属性输出` 槽位绑定的属性，视为该输入依赖集合共同影响的派生属性。
3. `配置输入` 槽位与 `系统输入` 槽位不产生新的属性依赖节点，但会成为规则执行上下文的一部分。
4. `只读输出` 不新增属性节点，但会形成派生结果。

## 10.3 与物品编辑页的联动

推荐流程如下：

1. 用户为物品添加某个属性
2. 系统读取该属性关联的规则绑定实例
3. 收集该规则实例中所有必填 `属性输入` 槽位
4. 若尚未出现在当前填写清单，则自动补齐
5. 运行表达式，生成输出结果
6. 只读输出显示为派生结果
7. 属性输出与系统输出按对应策略写回

自动补齐边界固定为：

- 只自动补齐必填的 `属性输入` 槽位
- 不自动补齐可选属性输入
- 不自动补齐配置输入
- 不自动补齐系统输入
- 不自动补齐规则输出

## 10.4 删除属性的影响分析

删除某个属性时，系统需要检查：

1. 它是否被用作某条规则实例的输入槽位
2. 它是否被用作某条规则实例的输出槽位
3. 删除后会导致哪些规则实例不完整
4. 删除后会影响哪些派生属性或只读输出

也就是说，删除影响分析应建立在“绑定实例 + 槽位角色”之上，而不是只看模糊规则名。

---

## 11. 模板在这套草案中的位置

模板不是主模型，而是创建母板。

在这套草案下，模板应分成两类：

1. 属性模板
2. 规则模板

其中：

- 属性模板负责帮助用户创建属性定义
- 规则模板负责帮助用户创建规则定义

也就是说：

- 属性模板负责“创建字段”
- 规则模板负责“创建跨字段计算”

---

## 12. 第一阶段固定边界

## 12.1 当前正式支持的方向

- 属性定义
- 属性值
- 表达式型规则定义
- 槽位化输入输出定义
- 规则绑定实例
- 只读输出
- 属性输出
- 系统输出
- 属性模板
- 规则模板
- 基于绑定实例的依赖推导

## 12.2 当前不优先支持的方向

- 独立关系模型
- 任意公式语言
- 任意脚本化规则
- 通用低代码平台能力
- 多阶段异步规则执行管线
- 完整决策表可视化编辑器

## 12.3 当前阶段最重要的平衡点

当前阶段最重要的不是“理论上最强”，而是：

- 简单属性足够轻
- 复杂规则足够清楚
- 用户知道什么时候是在建属性
- 用户知道什么时候是在建规则
- 系统知道如何从规则绑定中推导依赖与影响

---

## 13. 周期扣费示例

为了帮助统一理解，下面给出一个完整示例：

规则名：

- `周期扣费`

槽位：

1. `price`
   - 输入槽位
   - 数值
   - 属性输入
2. `chargeDay`
   - 输入槽位
   - 日期
   - 属性输入
   - 允许快捷创建属性
3. `startDate`
   - 输入槽位
   - 日期
   - 配置输入
4. `endDate`
   - 输入槽位
   - 日期
   - 配置输入
   - 可选
5. `cycleStep`
   - 输入槽位
   - 数值
   - 配置输入
6. `cycleUnit`
   - 输入槽位
   - 周期单位
   - 配置输入
7. `currentDate`
   - 输入槽位
   - 系统日期时间
   - 系统输入
8. `totalPaid`
   - 输出槽位
   - 数值
   - 属性输出
   - 允许快捷创建属性

表达式：

- `cycleCount(startDate, coalesce(endDate, currentDate), chargeDay, cycleStep, cycleUnit) * price`

从属性 `QQ会员价格` 出发绑定时：

- 当前属性先占据 `price`
- `chargeDay` 可通过快捷创建生成 `订阅日`
- `totalPaid` 可通过快捷创建生成 `累计总价`
- `startDate`、`cycleStep`、`cycleUnit` 作为配置输入补齐
- `currentDate` 由系统变量注入

最终形成一条完整的规则绑定实例。

---

## 14. 与当前实现的关系

当前代码中的规则实现仍然带有明显的第一阶段之前痕迹，例如：

- `RuleDefinition`
- `RuleBinding`
- `inputRoles`
- `requiredDependencies`
- `optionalDependencies`
- `outputKeys`

这些结构帮助系统先跑起来了，但它们还不足以完整表达：

- 槽位方向
- 槽位来源类型
- 配置输入
- 系统输入
- 输出落点
- 绑定实例完整性
- 快捷创建属性

因此下一阶段的方向应明确为：

1. 以 `Rule_Definition_Binding_Design_v1.md` 为准重构规则模型。
2. 让 `RuleDefinition` 演进为“规则定义 + 槽位定义 + 表达式定义”。
3. 让旧 `RuleBinding` 演进为真正的“规则绑定实例”。
4. 让规则详情页、规则编辑页、规则绑定页、物品编辑页统一使用同一套术语。

---

## 15. 当前草案的最终结论

当前可以先固定的草案如下：

1. 系统当前只正式建立两套主模型：属性模型与规则模型。
2. 属性模型包含：属性定义、属性值。
3. 属性定义当前先严格收敛为：
   - 属性名
   - 值类型
   - 值属性
   - 值来源
   - 交互方式
   - 规则绑定实例引用
4. 规则模型包含：规则定义、槽位定义、规则绑定实例、规则输出。
5. 规则的输入输出必须围绕“槽位”建模，而不是直接写死具体属性名。
6. 规则绑定的真正对象是“绑定实例”，不是简单的规则名列表。
7. 属性之间的关联关系，当前先隐含在规则绑定实例中，不单独建立关系模型。
8. 物品编辑页应基于规则绑定实例自动补齐缺失的必填属性输入槽位。
9. 删除属性时应基于绑定实例和槽位角色分析影响范围。
10. 模板不是主模型，而是创建母板，后续应明确分为属性模板与规则模板。
