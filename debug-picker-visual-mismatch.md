# [OPEN] picker-visual-mismatch

## 问题概述
- 现象：分类选择器的视觉效果与预期不符，风格和液态玻璃不一致，手机状态栏与底部控制条与 App 主体割裂。
- 期望：分类选择器应与分类管理页保持一致的 edge-to-edge 和液态玻璃风格，状态栏与底部系统区域都由页面自身自然承接。

## 当前已知事实
- 分类选择器当前使用全屏 `Dialog`，并设置了 `decorFitsSystemWindows = false`。
- 选择器已重构为固定头部、滚动内容区、固定底部操作区。
- 用户反馈最新修改在视觉上几乎没有起作用，说明问题可能不只在 Compose 结构层，也可能在 Dialog 窗口、主题承接或系统栏可见背景来源。

## 待验证假设
1. 全屏 `Dialog` 窗口本身没有继承或应用液态玻璃背景，导致状态栏和底部控制条后方显示的是默认窗口底色。
2. 选择器内部使用 `Surface(surfaceContainerLowest)` 作为整屏底色，视觉上天然偏“实色页面”，与项目的液态玻璃背景层不一致。
3. 底部操作区虽然处理了 `navigationBars` 和 `IME`，但底层没有与宿主一致的背景/模糊承接，因此系统导航区域仍显得割裂。
4. 头部和底部虽然在结构上 edge-to-edge，但中间列表与卡片材质、色彩和层次没有沿用当前项目的液态玻璃视觉语言。
5. 全屏 `Dialog` 的系统栏图标外观、scrim 或 window 背景与主 Activity 不一致，导致用户看到“系统栏和 App 格格不入”。

## 调试计划
1. 核对分类选择器所在 `Dialog`、主题与液态玻璃背景承接链。
2. 增加最小化运行时插桩，记录 `Dialog` 根视图、窗口背景、系统栏 inset、头部/底部实际尺寸与背景来源。
3. 结合用户截图与日志确认真正的视觉割裂来源。
4. 在证据明确后做最小范围修复，并进行前后对比验证。

## 当前状态
- 状态：进行中
- 下一步：阅读分类选择器相关实现与项目液态玻璃背景实现，随后添加运行时插桩。
