<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="64dp"
    android:background="?selectableItemBackground"
    android:clickable="true"
    android:focusable="true"
    android:padding="1dp">

    <!-- 选中状态背景 -->
    <View
        android:id="@+id/selectedBackground"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:background="@drawable/rounded_background"
        android:backgroundTint="@color/primary_light"
        android:visibility="gone"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- 日期数字 -->
    <TextView
        android:id="@+id/dayNumber"
        android:layout_width="32dp"
        android:layout_height="32dp"
        android:gravity="center"
        android:textSize="15sp"
        android:textStyle="bold"
        android:textColor="@color/text_primary"
        android:layout_marginTop="4dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_bias="0.5"
        tools:text="15" />

    <!-- 事件指示器容器 -->
    <LinearLayout
        android:id="@+id/eventIndicatorsContainer"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center"
        android:layout_marginTop="2dp"
        app:layout_constraintTop_toBottomOf="@id/dayNumber"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <!-- 事件指示器1 -->
        <View
            android:id="@+id/eventIndicator1"
            android:layout_width="5dp"
            android:layout_height="5dp"
            android:layout_marginEnd="1dp"
            android:background="@drawable/dot_indicator"
            android:visibility="gone" />

        <!-- 事件指示器2 -->
        <View
            android:id="@+id/eventIndicator2"
            android:layout_width="5dp"
            android:layout_height="5dp"
            android:layout_marginEnd="1dp"
            android:background="@drawable/dot_indicator"
            android:visibility="gone" />

        <!-- 事件指示器3 -->
        <View
            android:id="@+id/eventIndicator3"
            android:layout_width="5dp"
            android:layout_height="5dp"
            android:background="@drawable/dot_indicator"
            android:visibility="gone" />

    </LinearLayout>

    <!-- 更多事件提示 -->
    <TextView
        android:id="@+id/moreEventsText"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textSize="9sp"
        android:textColor="@color/text_secondary"
        android:textStyle="bold"
        android:visibility="gone"
        android:layout_marginTop="1dp"
        app:layout_constraintTop_toBottomOf="@id/eventIndicatorsContainer"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        tools:text="+2" />

</androidx.constraintlayout.widget.ConstraintLayout> 