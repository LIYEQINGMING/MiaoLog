package com.example.itemmanagement.utils

import android.content.Context

object VersionUpdateManager {
    fun shouldShowUpdateDialog(context: Context): Boolean {
        return false // Return false to bypass for now
    }
}